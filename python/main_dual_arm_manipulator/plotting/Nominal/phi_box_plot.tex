\documentclass[tikz, border=2mm]{standalone} % Use standalone class
\usepackage[utf8]{inputenc}

% --- Essential Packages ---
\usepackage{pgfplots}   
\pgfplotsset{compat=1.18} 
\usepackage{tikz}

% --- Define custom colors ---
\definecolor{pt_bright_red}{RGB}{230, 97, 74}
\definecolor{pt_bright_green}{RGB}{94, 201, 98}
\definecolor{pt_bright_blue}{RGB}{74, 144, 226}

% --- Math Commands ---
\newcommand{\vr}{\mathbf{r}}
\newcommand{\vt}{\mathbf{t}}

\begin{document}
	
	% Note: Since we are in standalone, we don't need the figure environment 
	% or \textwidth (which doesn't exist here). Use fixed units for width/height.
	
	\newcommand{\plotPath}{dual_arm_nominal.csv}
	
	\begin{tikzpicture}
		\begin{axis}[
			height=6cm,      % Use absolute units (cm/in) instead of \textheight
			width=10cm,      % Use absolute units instead of \textwidth
			xlabel={$t$ [s]},
			ylabel={$\varphi$ [deg]},
			grid=major,
			ymin=-8,
			ymax= 8,
			legend style={at={(0,1)}, anchor=north west},
			]
			
			\addplot[red, solid,line width=1.5] table [x=t, y expr=\thisrow{q_box_phi}*180/pi, col sep=comma] {\plotPath};
			\addplot[black, dashed,line width=1.5] table [x=t, y expr=\thisrow{ref_box_phi}*180/pi, col sep=comma] {\plotPath};
			
%			\addplot[blue, solid,line width=1.5]  table [x=t, y=q_box_phi, col sep=comma] {\plotPath};
			
			\addlegendentry{$\varphi_{\mathrm{box}}$}
			\addlegendentry{$\varphi^{\mathrm{ref}}_{\mathrm{box}}$}
		\end{axis}
	\end{tikzpicture}
	
\end{document}